import { login } from '@src/resolvers/Mutation/login'
import { post } from '@src/resolvers/Mutation/post'
import { signup } from '@src/resolvers/Mutation/signup'
import { vote } from '@src/resolvers/Mutation/vote'

export default {
  login,
  post,
  signup,
  vote,
}
